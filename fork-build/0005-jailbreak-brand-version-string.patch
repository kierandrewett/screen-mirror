--- a/meson.build	2026-04-14 16:00:45.000000000 +0100
+++ b/meson.build	2026-05-23 15:11:48.029189000 +0100
@@ -1,5 +1,9 @@
 project('gnome-shell', 'c',
-  version: '49.6',
+  # kierandrewett/gnome-shell jailbreak: build metadata suffix surfaces in
+  # `gnome-shell --version`, the org.gnome.Shell.ShellVersion D-Bus
+  # property, and Settings > About > GNOME Version, so it's obvious at a
+  # glance that this is the patched fork rather than stock 49.6.
+  version: '49.6+kdr',
   meson_version: '>= 1.3.0',
   license: 'GPL-2.0-or-later',
 )
--- a/subprojects/extensions-tool/meson.build	2026-04-14 16:00:45.000000000 +0100
+++ b/subprojects/extensions-tool/meson.build	2026-05-23 15:14:01.290513712 +0100
@@ -1,5 +1,7 @@
 project('gnome-extensions-tool', 'c',
-  version: '49.6',
+  # kierandrewett/gnome-shell jailbreak: must track parent project version
+  # so the version-match assertion in ../../meson.build passes.
+  version: '49.6+kdr',
   meson_version: '>= 0.58.0',
   license: 'GPL-2.0-or-later',
 )
--- a/subprojects/extensions-app/meson.build	2026-04-14 16:00:45.000000000 +0100
+++ b/subprojects/extensions-app/meson.build	2026-05-23 15:14:03.385553969 +0100
@@ -1,5 +1,7 @@
 project('gnome-extensions-app',
-  version: '49.6',
+  # kierandrewett/gnome-shell jailbreak: must track parent project version
+  # so the version-match assertion in ../../meson.build passes.
+  version: '49.6+kdr',
   meson_version: '>= 0.58.0',
   license: 'GPL-2.0-or-later',
 )
